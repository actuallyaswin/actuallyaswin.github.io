[pytest]
testpaths = tests
# Third-party plugins in the ambient environment (rerunfailures binds a socket;
# html/cov add noise) are not needed here and break in sandboxed shells.
addopts = -q -p no:rerunfailures -p no:cacheprovider
